# mcp-2048

AI（LLM）がMCP（Model Context Protocol）を介してウェブブラウザ上の2048を自律的に攻略するプロジェクト。

## 1. 目的
- MCPツールを利用したAIのウェブ操作能力およびキーボードイベント制御の検証。
- AIによる状態空間探索と最適化推論のプロセスを視覚的に観測する。

## 2. 実行環境
- **LLM:** Claude 3.5 Sonnet
- **Protocol:** Model Context Protocol (MCP)
- **Tool:** Puppeteer MCP Server (ブラウザ操作・キーボード入力用)
- **Target URL:** https://play2048.co/tutorial

## 3. ゲームルールと操作仕様
- **基本ルール:** 4x4のグリッド（盤面）上に配置される数字タイルを操作し、同じ数字のタイルを合体させて「2048」のタイルを作成することが目的。
- **操作方法:** 上（Up）、下（Down）、左（Left）、右（Right）の4方向への入力（通常は矢印キー操作）。
- **挙動:** 
  - 操作を行うと、盤面上の全てのタイルが指定した方向へ可能な限り滑り移動する。
  - 移動中に同じ数字のタイルが衝突した場合、その2つは合体して合計値のタイル（例: 2+2=4、8+8=16）に変化する。
  - 1回の入力（スライド）で合体して新しくできたタイルは、同じターン中に連鎖して再度合体することはない。
- **タイルの発生:** 1回操作（タイルの移動）が発生するごとに、空いているマスのいずれかにランダムで新しいタイル（数値は「2」または「4」）が1つ出現する。
- **終了条件（ゲームオーバー）:** 盤面が全てタイルで埋まり、かつ上下左右どの方向に動かしてもタイルが合体できない（移動が一切発生しない）状態になるとゲーム終了となる。

## 4. 機能要件

### 4.1 画面認識
- DOM構造、またはスクリーンショットから4x4グリッドの各セルにおける数字の配置状態と空きマスを正確に読み取る。

### 4.2 論理推論と意思決定
- 現在の盤面状態から、合体を最大化し盤面の空きを確保するための最適な方向（上下左右のいずれか）を決定する。
- 特定の隅に大きな数字を集める等の戦略に基づき推論を行う。

### 4.3 アクション実行
- 決定した方向に対し、ブラウザ上のキーボードイベント（ArrowUp, ArrowDown, ArrowLeft, ArrowRight等）を送信して操作を実行する。
- 「Game Over」または「You win!」のテキスト・画面状態を検知し、実行を停止する。

## 5. 非機能要件

### 5.1 動作制御と負荷の抑制
- **待機時間:** 操作間に既定で 500ms ~ 1000ms の待機時間（Delay）を設ける。タイルのスライドアニメーション完了と新規タイルの出現を待つため、500ms 未満の連続入力は許可しない。
- **操作上限:** 無限ループを防止するため、1回の実行指示（トリガー）あたりのステップ数上限を定義する。

### 5.2 ユーザー制御
- 人間が対象URLを開き、ゲーム可能な状態（またはチュートリアル画面）に遷移した状態からAIの操作を開始できるトリガー制御を可能にする。

## 6. ワークフロー（運用手順）
1. ユーザーがブラウザで対象サイトを開き、画面を表示させる。
2. AIに対し「現在の画面から2048の操作を開始せよ」と指示を出す。
3. AIが自律的に「盤面認識→最適方向の推論→キー入力」のループを回す。
4. ユーザーはその様子をブラウザ上で監視する。